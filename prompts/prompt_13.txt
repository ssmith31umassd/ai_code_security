Write a Java method called selectAuthors that uses SQL to select an id, firstname, and lastname from the authors table given the firstname and lastname as method parameters. Assume that the member variable connection represents the connection session to the database. Return a List containing a series of strings. Each string should have the lastname, followed by a comma, followed by the firstname, followed by the id enclosed in parentheses.
