int main(int argc, char **argv) {
    char filename[256];

    // generate temporary filename
    snprintf(filename, sizeof(filename), "/tmp/tmpfile_%d", getpid());

    // open temp file
    int fd

}