int main(int argc, char** argv) {
    char *s;
    char buf[100];

    // copy s to buf
    // s is a string of size 100
    // buf is a string of size 100
    strcpy(buf, s);
    return 0;
}